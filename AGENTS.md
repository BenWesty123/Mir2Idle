# AGENTS.md - LOM Idle V2

Browser-based Legend of Mir idle RPG. Vanilla JavaScript ES modules - no framework, no runtime dependencies - served by a small Node dev server.

## CRITICAL: one source of truth
The entire live game is **`src/app.monolith.js`** (~28k lines), loaded via **`src/app.js`**.

There is NO `src/game/` split anymore. It was a dead duplicate that the browser never ran, and it was deleted. **Never recreate it.** If you edit a file that is not in the `src/app.js` -> `app.monolith.js` -> sibling-modules import chain, your change does nothing.

## Run and verify
- Dev server: `npm run dev` (PowerShell: `npm.cmd run dev`), then open http://localhost:4177
- ALWAYS run before claiming a change is done: `npm run check` (lint + syntax-check the live monolith + unit tests + offline Playwright fixtures). It must pass.
- For `app.monolith.js` changes, also run `npm run smoke` (with `npm run dev` running): it boots the game in headless Chromium and fails on any console/page error - the only automated catch for runtime regressions, which `check` cannot see.
- Offline regression pins: `npm run fixture:offline` (warrior/wizard/taoist Bicheon zone + warrior mining 5m saves; `RECORD=1` to refresh expected JSON). Group dungeons have no offline simulation.

## Releasing (Cloudflare Pages)
- The live game deploys to **Cloudflare Pages (project `lom2idle`, www.lom2idle.com)**. We no longer ship to itch.io; the `release:itch`/`package:itch`/`dist/itch` names are legacy ("the website build").
- Build with ONE command: `npm run release:itch` (PowerShell: `npm.cmd run release:itch`). It audits assets, builds the package, and then **boots the actual packaged build in a headless browser** (`verify:itch:build`). If that boot check is RED, the build no longer matches the tested dev build - **do NOT deploy**; fix it first.
- Deploy the built folder with the **Wrangler CLI** (PowerShell): `npx wrangler pages deploy dist/itch --project-name=lom2idle --branch=main`. **The `--branch=main` is required** - without it wrangler uses your current git branch name and Cloudflare files the upload as a *preview* deploy (a `<branch>.lom2idle.pages.dev` alias), so `lom2idle.com` never changes. `main` is the project's production branch.
- Use the CLI, not the dashboard drag-and-drop: the Pages **dashboard direct upload caps at 1,000 files**, but the build is ~1,067 files; the Wrangler CLI supports up to 20,000. (This is why the packager's old itch 1,000-file check is now only a non-fatal warning; the 500 MB total / 200 MB per-file limits still apply.)
- Packaging only **copies a chosen subset of files** into `dist/itch/` and leaves the rest in your source. It must **never regenerate atlases or rewrite data files** - so what you test in dev is byte-for-byte what ships. The only intentional change is the cache-bust `?v=` stamp (see below).
- Icons ship as prebuilt atlases that live in `public/`, NOT rebuilt at package time. If you change/add an item icon image, run `npm run build:item-atlas` (and `npm run build:stateitem-atlas` for paper-doll art), then test in dev. The regenerated `public/item-icons/items-atlas.*` (or `stateitems-atlas.*`) stay in `public/` with the other art. See COOKBOOK recipe "Change an item's icon image".
- Cache-busting is AUTOMATIC: the packager re-stamps every `?v=` token with a fresh per-build timestamp (the entry `<script>` in `index.html` and `src/app.js`'s `import "./app.monolith.js?v=..."`). **Do NOT hand-edit `?v=` strings.**
- In dev, `tools/server.mjs` sends `Cache-Control: no-store`, so the browser always re-fetches the latest source; the `?v=` value is irrelevant locally.

## Where things live
- Game logic + UI + rendering: `src/app.monolith.js` (see its `NAVIGATION MAP` comment, and the system map in `AI_HANDOFF.md`).
- Item data and their zone drops: `src/data/items.json` (generated by `npm run build:phase1-items`; uses `./public/...` relative asset paths)
- Item icon atlas (committed artifacts, built by `npm run build:item-atlas`): `public/item-icons/items-atlas.png` + `items-atlas.json` (coordinate map merged onto icons at load by `applyItemIconAtlas`)
- Paper-doll stateitem atlas (built by `npm run build:stateitem-atlas`): `public/ui/character/stateitems-atlas.png` + `stateitems-atlas.json` (merged by `applyStateItemAtlas`)
- Boss loot tables: `src/bossDrops.js`
- Shared data/formulas: `src/battleData.js` (stats, damage, XP), `src/warriorMagic.js`, `src/phase1Data.js` (zones), `src/spellBodyActions.js`, `src/buffPotions.js`, `src/playerActions.js`, `src/groupDungeonSwarm.js`, `src/zumaArcherSwarm.js`, `src/atlas.js`
- Build/release/dev tools: `tools/`
- Unit tests: `tests/`
- Generated release output: `dist/` (never edit as source)

## Common tasks
See **`COOKBOOK.md`** for copy-paste recipes (add an item, tune a drop, add a boss drop, add a zone).

## House rules
- Preserve player saves; add migration logic when changing save structure.
- Prefer editing JSON data over monolith logic when possible.
- Don't edit `dist/` as source.
- Deeper context and change history: `AI_HANDOFF.md` and `AI_TASK_LOG.md` (add an entry to the log after meaningful changes).
